{
  "name": "billing-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:django": ".\\.venv\\Scripts\\python.exe django_backend\\manage.py runserver",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "build:frontend": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "check": "npm run lint && npm run build",
    "start": "npm run dev",
    "vercel-build": "npm --prefix frontend install --no-audit --no-fund && npm --prefix frontend run build"
  },
  "engines": {
    "node": "20.x"
  }
}
